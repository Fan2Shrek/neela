framework:
    cache:
        # Unique name of your app: used to compute stable namespaces for cache keys.
        #prefix_seed: your_vendor_name/app_name

        # The default filesystem adapter is local to whichever container/host writes
        # to it — the web process and the messenger worker never see the same files,
        # so cache invalidation from one side is invisible to the other. Use the
        # already-shared Postgres connection instead (no extra infra, same reasoning
        # as the "failed" messenger transport using doctrine:// for the same purpose).
        app: cache.adapter.doctrine_dbal

        # Redis
        #app: cache.adapter.redis
        #default_redis_provider: redis://localhost

        # APCu (not recommended with heavy random-write workloads as memory fragmentation can cause perf issues)
        #app: cache.adapter.apcu

        # Namespaced pools use the above "app" backend by default
        #pools:
            #my.dedicated.cache: null
